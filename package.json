{
  "name": "careerpilot-frontend-wrapper",
  "version": "1.0.0",
  "description": "Wrapper for deploying frontend to Heroku",
  "engines": {
    "node": "16.x",
    "npm": "8.x"
  },
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd frontend && npm start",
    "heroku-postbuild": "cd frontend && npm install && npm run build"
  }
}